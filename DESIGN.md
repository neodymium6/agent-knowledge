# Agent Knowledge Design

## 1. Status

This document is the design baseline for Agent Knowledge. It describes the
initial implementation and the invariants that later implementations must
preserve.

The implementation language is Rust. The initial deployment target is a
conventional Linux host with a local POSIX-style file system. The design should
remain deployable on Kubernetes, but Kubernetes deployment manifests and
horizontal scaling are not part of the initial release.

## 2. Purpose

Agent Knowledge collects work logs, decisions, experiments, runbooks,
references, and related artifacts produced by coding agents on multiple
machines.

The primary clients are command-line agents such as Codex running on
supercomputers, notebooks, and desktops. They must be able to:

- submit Markdown documents and attachments;
- retrieve committed Markdown and attachments;
- search by text and metadata;
- organize knowledge primarily through directories;
- use tags and front matter for secondary classification; and
- check the status of submitted changes.

Client machines do not clone, pull, fetch, rebase, or push the knowledge Git
repository.

## 3. Goals

- Keep Markdown and ordinary attachment files as the authoritative knowledge
  representation.
- Preserve a directory hierarchy that remains useful without this software.
- Accept images, CSV, JSON, PDF, and HTML attachments.
- Provide a restricted SSH interface for coding agents and shell users.
- Provide human-readable pages generated by Quartz.
- Preserve every accepted request durably before acknowledging it.
- Apply each change request completely or not at all.
- Detect concurrent updates without silently overwriting data.
- Record committed history and support remote Git backup.
- Avoid product-specific storage formats and database requirements.
- Keep Git, Quartz, and search implementations replaceable.

## 4. Non-goals

The initial release does not provide:

- a general-purpose SSH shell;
- Git access for client nodes;
- physical deletion through the client API;
- collaborative pull-request workflows;
- Web authentication, SSO, VPN, TLS termination, or user management;
- a database-backed source of truth;
- semantic or vector search;
- automatic classification, summarization, or title generation with an LLM;
- extraction or indexing of text inside binary attachments;
- multiple Repository Workers writing concurrently; or
- a highly available, horizontally scaled Kubernetes deployment.

## 5. Invariants

The following rules are architectural invariants, not configuration options:

1. The authoritative knowledge representation is a hierarchy of files.
2. The Gateway does not use an LLM.
3. The Gateway never modifies `content/`.
4. Only the Repository Worker modifies authoritative content.
5. One change request either succeeds in full or has no content effect.
6. Logs are append-only by default.
7. Updates to mutable documents use optimistic concurrency control.
8. The initial API does not provide physical deletion.
9. Git operations run only on the central server.
10. Multiple successful requests may be grouped into one commit.
11. Commit messages list request titles mechanically.
12. Quartz is a replaceable static-site generator.
13. Web access control is outside this project.
14. Normal reads and searches use only a committed revision.
15. Accepted request contents are never silently discarded.

## 6. Technology decisions

### 6.1 Rust

Gateway, Worker, client CLI, and administrative commands are implemented in one
Rust workspace. They share domain types, validation rules, path handling,
queue-state transitions, revision calculation, and error codes.

The initial release produces one executable with subcommands:

```text
agent-knowledge client ...
agent-knowledge gateway ...
agent-knowledge worker ...
agent-knowledge admin ...
```

The implementation bootstrap exposes one local-only administrative intake
command before the SSH Gateway is added:

```text
agent-knowledge admin submit \
  --queue-root /srv/agent-knowledge/queue \
  --package-root ./fictional-request
```

`package-root` contains extracted `request.json` and `payload/` entries. The
command validates that directory, restreams every permitted file through the
same `FileQueue` limits as the future Gateway, and prints one JSON acceptance
result. It never copies an unchecked directory into an accepted queue state.

The same executable can be used with different entry-point arguments in a
container. Separate binaries may be produced from the same workspace later,
without changing protocol or domain logic.

The implementation starts with synchronous I/O. OpenSSH creates a Gateway
process for each connection, and the single Worker does not require an
asynchronous runtime. An async runtime must only be added after a demonstrated
need.

Production code must not use `unsafe` unless a later design decision documents
and justifies a specific use.

### 6.2 External programs

The system uses installed programs for capabilities that already have mature
implementations:

- OpenSSH for transport and public-key authentication;
- Git for history, local recovery, and remote backup; and
- Quartz for static-site generation.

The Rust application invokes these programs with argument arrays. It must not
construct shell command strings from request data.

### 6.3 Linux and Kubernetes

The baseline environment is a normal Linux server. The storage implementation
requires:

- atomic rename within a file system;
- durable file and directory synchronization;
- advisory file locking; and
- normal POSIX file and directory semantics.

All durable paths are configurable. Processes handle termination signals and
shut down at transaction boundaries.

A future Kubernetes deployment uses one StatefulSet replica and one persistent
volume mounted by a single Pod. The volume must provide the file-system
semantics above. Kubernetes compatibility does not imply initial support for
multiple Gateway or Worker replicas.

SSH host keys, client public keys, Git credentials, and other secrets are
deployment inputs. They are never stored in this repository or in committed
configuration examples.

## 7. Architecture

```text
┌─────────────────────────────────────┐
│ Client nodes                        │
│ Codex / coding agents / shell users │
└──────────────────┬──────────────────┘
                   │ OpenSSH public-key authentication
                   ▼
┌─────────────────────────────────────┐
│ Gateway                             │
│ authenticate, validate, enqueue,    │
│ read, search, and report status     │
└──────────────────┬──────────────────┘
                   │ durable request package
                   ▼
┌─────────────────────────────────────┐
│ Persistent request queue            │
│ pending / processing / completed /  │
│ failed                              │
└──────────────────┬──────────────────┘
                   ▼
┌─────────────────────────────────────┐
│ Repository Worker                   │
│ validate, apply, build, commit,      │
│ publish, and replicate              │
└──────────────┬──────────────────────┘
               │
         ┌─────┴──────────┐
         ▼                ▼
┌─────────────────┐  ┌─────────────────┐
│ Knowledge files │  │ Git remote      │
│ and local Git   │  │ remote backup   │
└────────┬────────┘  └─────────────────┘
         ▼
┌─────────────────┐
│ Quartz release  │
└─────────────────┘
```

### 7.1 Gateway

The Gateway:

- receives only forced-command SSH sessions;
- derives a client ID from the authenticated public key configuration;
- validates the wire format and operation;
- enforces request, file-count, and byte limits;
- rejects unsafe archive entries and paths;
- validates request metadata and front matter;
- derives create destinations from fixed rules;
- assigns incomplete classification to `inbox/`;
- checks request idempotency;
- durably stores accepted changes in `queue/pending/`;
- serves committed documents and attachments;
- searches committed Markdown and permitted metadata; and
- reports request status.

The Gateway does not write `content/`, run Git, or run Quartz.

### 7.2 Repository Worker

The Repository Worker is the only writer to authoritative content. It:

- holds the repository writer lock;
- claims pending requests;
- recovers interrupted processing;
- revalidates every request;
- checks optimistic-concurrency preconditions;
- applies operations to temporary Git worktrees;
- validates resulting content;
- performs a trial Quartz build;
- creates a Git commit for successful requests;
- advances the official branch;
- updates the canonical checkout;
- creates and activates a release;
- marks requests completed or failed; and
- retries transient publication and Git-remote failures.

### 7.3 Quartz

Quartz receives a committed content tree and produces a static site in a
temporary release directory. It never changes authoritative content.

The Quartz command, configuration path, and integration directory are
configuration values. A trial build must finish successfully before a release
can become current.

### 7.4 Git remote

The remote is an asynchronous backup target. A remote push failure does not
roll back a locally committed and published change. The Worker records that the
remote is behind and retries the push.

## 8. Storage layout

```text
knowledge/
├── content/                 # canonical committed checkout
├── queue/
│   ├── incoming/            # incomplete Gateway writes
│   ├── quarantine/          # inactive incomplete packages awaiting reap
│   ├── pending/
│   ├── processing/
│   ├── completed/
│   ├── failed/
│   └── .locks/                # fixed queue and Worker lock identities
├── repository/              # bare Git repository
├── releases/
│   ├── by-id/
│   └── current -> by-id/<release-id>/
└── work/                    # disposable worktrees and builds
```

`repository/` is a bare Git repository. `content/` is its canonical linked
worktree. Transaction branches and trial builds live under `work/`.

The committed file revision is the visible knowledge state. The Gateway pins a
commit ID at the beginning of a read operation, so it never combines files from
two revisions.

The authoritative knowledge is the committed file hierarchy. The request queue
and local Git history are durable operational records. These items are derived
and can be rebuilt:

- Quartz output;
- search indexes;
- temporary worktrees; and
- the Git remote copy.

The entire `knowledge/` tree is placed on one file system in the initial
deployment. This makes queue transitions and release activation atomic.

## 9. Content hierarchy

Directories provide primary classification. Tags and front matter provide
secondary classification.

```text
content/
├── index.md
├── inbox/
│   └── YYYY/
│       └── MM/
├── projects/
│   └── <project>/
│       ├── index.md
│       ├── logs/
│       │   └── YYYY/
│       │       └── MM/
│       │           └── DD/
│       ├── experiments/
│       ├── decisions/
│       ├── runbooks/
│       ├── references/
│       ├── assets/
│       └── archive/
├── infrastructure/
│   ├── supercomputer/
│   ├── desktops/
│   ├── notebooks/
│   └── services/
├── shared/
│   ├── coding/
│   ├── prompts/
│   ├── operations/
│   └── research/
└── archive/
```

Node names are metadata and are not the top-level classification. Project
membership normally takes precedence.

### 9.1 Document types

#### Logs

- One work session normally creates one file.
- Logs are append-only.
- Failed attempts and intermediate reasoning may be retained.
- Existing logs are not updated, moved, or archived by normal client
  operations.
- A correction is a new document that links to the original.

#### Experiments

- Experiment documents and their artifacts share a directory.
- An experiment can be updated while active.
- A completed experiment can be marked immutable.

#### Decisions

- Decision records are mutable.
- They link to supporting logs, experiments, and references.

#### Runbooks

- Runbooks are mutable.
- Every update requires an expected revision.

#### References

- References hold external research and related information.

#### Project index

- `index.md` describes a project's purpose and current state.
- It is mutable and protected by optimistic concurrency.

## 10. Markdown and attachments

Markdown is the normal document format. Attachments are stored next to the
document or in the project's shared `assets/` directory.

```text
experiments/
└── 2026-07-31-gpu-memory/
    ├── index.md
    ├── memory-usage.png
    ├── results.csv
    ├── raw-results.json
    ├── profiler-report.html
    └── report.pdf
```

Initial attachment categories are:

- PNG, JPEG, and SVG images;
- CSV and JSON machine-readable data;
- PDF reports;
- HTML reports; and
- other explicitly configured extensions.

Standalone HTML is an attachment, not a replacement for a Markdown document.
It is linked from Markdown and is not parsed as knowledge content. The static
site integration must not treat attachment HTML as a Quartz template or
configuration source.

Executable bits, symbolic links, hard links, devices, sockets, and named pipes
are rejected.

## 11. Document metadata

Every Markdown document has YAML front matter. The initial schema includes:

```yaml
---
schema_version: 1
document_id: 01K00000000000000000000000
title: GPU memory measurement
created: 2026-07-31T03:50:00+09:00
updated: 2026-07-31T03:50:00+09:00
node: fictional-supercomputer-a
agent: codex
session: 01K00000000000000000000001
request_id: 01K00000000000000000000002
tags:
  - cuda
  - performance
status: active
---
```

`document_id` is a permanent ULID. Moving or archiving a document does not
change it. Paths describe current classification and may change.

The exact required fields vary by operation and document type:

- `schema_version`, `document_id`, `title`, `created`, `request_id`, and
  `status` are required for every document;
- log submissions also require `node`, `agent`, and `session`;
- `updated` is required after the first update; and
- `tags` may be empty.

Timestamps use RFC 3339 with an explicit offset. The Gateway verifies
client-supplied timestamps and records its own acceptance timestamp in the
request package.

Initial status values are:

- `active`;
- `completed`;
- `deprecated`; and
- `archived`.

A deprecated document may specify:

```yaml
status: deprecated
superseded_by: 01K00000000000000000000003
```

## 12. Path rules

Clients describe intent and metadata. They do not supply an unrestricted
destination path for new documents.

For example:

```text
project = cuda-solver
document_type = log
created = 2026-07-31T03:50:00+09:00

projects/cuda-solver/logs/2026/07/31/
```

If classification is incomplete but otherwise valid, the Gateway selects a
date-based path below `inbox/`. Explicitly invalid classification is rejected.

Path validation occurs independently in the Gateway and Worker using the same
shared Rust library. It:

- rejects absolute paths;
- rejects empty, `.` and `..` components;
- rejects NUL and control characters;
- rejects platform-specific path prefixes;
- rejects symbolic-link traversal;
- limits component and total path length;
- uses an allowlist for project and document-type identifiers;
- prevents case-folding collisions where relevant; and
- verifies that every resolved path remains below its assigned root.

Project identifiers use a conservative lowercase ASCII slug. Attachment file
names use a conservative configured character set. Display titles remain
Unicode and are not used as trusted paths.

## 13. SSH transport

### 13.1 OpenSSH

The project does not implement an SSH server. A dedicated operating-system
account uses OpenSSH public-key authentication and per-key forced commands.

A fictional `authorized_keys` entry has this form:

```text
restrict,command="/usr/local/bin/agent-knowledge gateway --client-id fictional-node-a" ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIFictionalKeyMaterialOnly
```

Each key maps to one configured client ID. The Gateway overwrites or rejects
any client-supplied identity that conflicts with the authenticated identity.

The OpenSSH configuration also disables password authentication and all
forwarding for the dedicated account. No interactive shell is available.

### 13.2 Operation selection

The official client runs the system `ssh` executable without a shell. The
forced Gateway receives the requested operation through
`SSH_ORIGINAL_COMMAND`. It accepts only an exact, versioned grammar such as:

```text
akp-v1 submit
akp-v1 status
akp-v1 list
akp-v1 recent
akp-v1 get
akp-v1 export
akp-v1 search
```

The Gateway parses this string itself. It never evaluates it as a shell
command.

### 13.3 Encoding

Control operations use one UTF-8 JSON request on standard input and one UTF-8
JSON response on standard output.

`submit` accepts an uncompressed tar stream containing exactly:

```text
request.json
payload/<regular-files>
```

Archive entries must have normalized relative paths. Only regular files and
directories are accepted. Duplicate names, sparse files, links, devices,
unexpected top-level entries, excessive counts, and limit violations are
rejected.

`export` returns an uncompressed tar stream for a selected document bundle.
Other read operations return JSON. Protocol errors are emitted as structured
JSON on standard error with a nonzero process exit status.

The protocol is versioned independently from document front matter.

## 14. Change requests

A change request represents one logical, atomic user change:

```text
ChangeRequest
├── protocol_version
├── request_id
├── title
├── project
├── document_type
├── node
├── agent
├── session
├── created_at
└── operations
```

Every request has a meaningful, client-supplied title. The central service
never generates a title with an LLM.

The official client generates the request ULID before opening the SSH
connection. This lets it safely retry after a lost response. A write request
without a valid request ID is rejected.

Initial operations are:

- create a Markdown document;
- update a mutable Markdown document;
- move a mutable document;
- archive a mutable document; and
- add an attachment.

Physical deletion and attachment overwrite are not initial operations.

Each operation declares all preconditions. Updates, moves, and archives
identify a document by `document_id`, not only by path.

## 15. Idempotency

The Gateway calculates a digest of the normalized request metadata and payload.
It searches all queue states for the request ID.

- A new request ID is accepted normally.
- An existing ID with the same digest is revalidated as an immutable accepted
  package before the Gateway returns its existing status.
- An existing ID with a different digest fails with
  `REQUEST_ID_REUSED`.

The existing accepted package is revalidated before either digest comparison.
A malformed stored digest or a mismatch between stored and calculated
immutable contents is queue corruption, not request ID reuse.

The Worker records request IDs in machine-readable Git commit trailers. Crash
recovery checks both queue state and committed history before applying a
request.

## 16. Durable queue

### 16.1 Package layout

Accepted request data is immutable:

```text
pending/
└── 01K00000000000000000000000/
    ├── request.json
    ├── digest
    ├── acceptance.json  # immutable Gateway sequence and timestamp
    ├── phase.json       # optional Worker-owned sidecar
    ├── result.json      # optional Worker-owned sidecar
    └── payload/
        ├── index.md
        ├── graph.png
        └── results.csv
```

`request.json`, `digest`, `acceptance.json`, and `payload/` are immutable.
`acceptance.json` contains the central acceptance timestamp and a queue-local
monotonic sequence allocated under the queue lock. Gaps are allowed after an
interrupted acceptance, but accepted packages never share a sequence. Worker
state and results use only the optional `phase.json` and `result.json`
sidecars next to that immutable data. The Gateway never creates these
sidecars. The Worker writes them through temporary files and atomic rename;
package revalidation excludes Gateway and Worker metadata bytes from the
immutable client-package digest but still rejects links, executable files,
and any unknown top-level entry.

The next sequence is stored durably in `queue/next-sequence`. If that file is
missing while any accepted-state directory is nonempty, queue initialization
fails instead of restarting at one. Operators must recover the sequence state
from durable queue metadata before accepting another request.

### 16.2 Durable acceptance

The Gateway accepts a request as follows:

1. While holding the queue lock, create a randomly named directory below
   `queue/incoming/`, acquire its advisory lease, and then release the queue
   lock.
2. Stream files while enforcing count and byte limits.
3. Validate the complete package and calculate its digest.
4. Synchronize every file.
5. Synchronize the payload and package directories.
6. Allocate and durably record the next acceptance sequence under the queue
   lock.
7. Write and synchronize `acceptance.json` with that sequence and the central
   acceptance timestamp.
8. Atomically rename the package to `queue/pending/<request-id>/`.
9. Synchronize the `pending/` directory.
10. Return success.

Gateway success therefore means that the request survives a normal process or
host crash according to the guarantees of the configured storage.

All queue-state directories must be on the same file system. Moving a request
between states uses atomic rename followed by parent-directory
synchronization.

### 16.3 State machine

```text
incoming ──accepted──> pending ──claimed──> processing
                           ▲                    │
                           │ transient failure  ├──success──> completed
                           └────────────────────┤
                                                └──permanent failure──> failed
```

`incoming/` is not an accepted state. Stale incomplete incoming directories
are protected by an advisory lease while active. An explicit administrative
maintenance operation may atomically move only inactive entries older than a
configured threshold into `quarantine/`. A separate operation may remove
stale quarantined entries. Neither operation scans or removes accepted
packages. Each maintenance invocation has explicit maximum scan and action
counts so a large abandoned directory set cannot cause unbounded work or
memory use. Marker creation or repair consumes the same action budget as a
move or reap. A long-running queue handle retains each directory iterator
between invocations, so bounded scans resume after the previous entry instead
of repeatedly inspecting the same prefix.

After the atomic quarantine rename, maintenance atomically writes and
synchronizes a completed `.quarantined-at` marker. Reaping validates the
marker and measures retention age from its modification time rather than from
the incoming directory's older modification time. If a crash leaves the
quarantined directory without a complete marker, the next reap invocation
atomically creates a fresh marker and retains the directory for the full
configured period.

`processing/` entries contain an atomic phase record. On startup, the Worker
examines interrupted entries, commit trailers, the official branch, and
release metadata. It either resumes the next idempotent phase or returns the
request to `pending/`.

Every Worker queue transition requires an exclusive Worker session. Session
creation takes the fixed `queue/.locks/repository-writer.lock` without waiting
and fails if another Worker owns it. This lock is separate from the fixed,
short-lived `queue/.locks/queue.lock`. Because both identities are derived from
the queue root rather than supplied by callers, independently initialized
handles for the same queue cannot select different lock files. The Gateway can
continue accepting requests while the Worker is otherwise idle or applying a
batch.

Pending selection takes a fixed acceptance-sequence snapshot and incrementally
walks the pending directory. Each call has a maximum number of directory
entries to inspect, and the scanner retains only the earliest configured number
of candidates in memory. Selection finishes the complete snapshot before
claiming, then orders retained requests by acceptance sequence and request ID.
The queue lock is held only while taking the snapshot and while performing
state transitions, not while reading and hashing immutable packages. Requests
accepted after the snapshot began remain pending for the next batch.

A pending package that has a canonical request ID but fails deterministic
immutable validation is given an atomic `result.json` and moved to `failed/`.
The scanner then continues, so one damaged request cannot indefinitely block
unrelated valid work. Structurally invalid queue entries that cannot be safely
identified as requests remain operator-visible corruption and stop the scan.

The initial claim record is a bounded, versioned `phase.json` sidecar:

```json
{
  "schema_version": 1,
  "request_id": "01K00000000000000000000000",
  "batch_id": "01K00000000000000000000010",
  "attempt": 1,
  "phase": "claimed",
  "updated_at": "2026-07-31T04:00:00Z"
}
```

Claiming one request validates and hashes the accepted package before taking the
queue lock. While holding the lock it rechecks the state, digest, and bounded
phase metadata, atomically writes and synchronizes this record through
`queue/worker-tmp/`, renames `pending/<request-id>/` to
`processing/<request-id>/`, and synchronizes both state directories. Batch
claims acquire and release the queue lock per request. A retry after a
phase-only interrupted claim advances the attempt number. A request already
renamed to `processing/` keeps enough durable ownership metadata to be
recovered.

Returning a claimed request to `pending/` requires an exact token containing
the request ID, batch ID, and attempt. This prevents a stale Worker from
requeueing a later attempt owned by another batch. The phase record remains in
the package so the next claim can advance the attempt monotonically.

After acquiring the writer lock, a replacement Worker incrementally scans
`processing/` with an explicit entry limit. It reconstructs exact claim tokens
from validated packages and durable phase records. No other Worker transition
is allowed until that recovery scan completes successfully; an error leaves the
session recovery-gated. This also recovers requests for which rename succeeded
but the previous process failed before returning a token or completing
directory synchronization.

## 17. Revisions and conflicts

Mutable Markdown updates use an exact SHA-256 digest of the current file bytes:

```text
document_id: 01K00000000000000000000000
expected_revision: sha256:0123456789abcdef...
```

The Worker resolves `document_id` in the pinned base commit and verifies:

- exactly one document has the ID;
- the operation is permitted for its document type and status; and
- its digest equals `expected_revision`.

A mismatch fails with `REVISION_CONFLICT`. The existing content is never
silently overwritten.

New log creation does not require an expected revision. Creation still fails
if the generated path, document ID, or attachment path already exists.

Requests within a batch are evaluated in durable acceptance order, with the
request ID as a deterministic tie-breaker. If two requests update the same
revision, the first may succeed and the second fails with a conflict.

## 18. Worker transaction

The Worker performs these phases while holding the writer lock:

1. Recover interrupted processing.
2. Select a bounded batch of pending requests.
3. Atomically move selected requests to `processing/`.
4. Pin the current official commit.
5. Create a temporary worktree and transaction branch.
6. Revalidate and apply each request in deterministic order.
7. Remove permanently invalid requests from the candidate batch.
8. Validate the resulting file hierarchy and Markdown.
9. Build Quartz into a temporary directory.
10. Create one Git commit for the successful requests.
11. Atomically advance the official branch with an expected-old-commit check.
12. Synchronize the canonical `content/` worktree.
13. finalize and activate the new Quartz release.
14. Mark successful requests `completed`.
15. Mark permanent request failures `failed`.
16. Schedule or perform the Git remote push.
17. Remove disposable worktrees after their results are no longer needed.

The branch update uses compare-and-swap semantics. An unexpected branch change
stops publication and requires recovery; it is never overwritten.

The canonical checkout and release can be repaired from the official commit
after a crash. Normal readers use the pinned official commit, not an
in-progress checkout update.

## 19. Batch isolation

Atomicity is defined per request, not per commit batch. One invalid request
must not make unrelated valid requests fail.

The Worker applies and validates requests sequentially in a temporary
worktree. A request that fails its own validation is rolled back in that
worktree and moved to `failed/`. Remaining requests continue.

If the final Quartz build fails:

- a clearly transient process or I/O failure returns all candidate requests to
  `pending/` for retry;
- a deterministic content failure triggers replay with smaller groups and,
  when necessary, individual requests;
- only requests proven to cause the failure are marked failed; and
- unaffected requests are rebuilt and committed together.

This fallback is slower than the normal batch path but preserves request-level
isolation.

## 20. Commit policy

A batch closes when any configured threshold is reached:

- no new request has arrived for the debounce interval;
- the oldest uncommitted request reaches the maximum batch age; or
- the batch reaches the maximum request count.

Initial example values are 30 seconds, 5 minutes, and 100 requests. They remain
configuration values.

Commit messages are generated mechanically:

```text
knowledge snapshot: 3 changes

- [01K00000000000000000000001] Add CUDA OOM investigation
- [01K00000000000000000000002] Add GPU memory usage graph
- [01K00000000000000000000003] Record Slurm retry procedure

Requests: 3
Files-Added: 6
Files-Modified: 1
Files-Deleted: 0

Knowledge-Request: 01K00000000000000000000001
Knowledge-Request: 01K00000000000000000000002
Knowledge-Request: 01K00000000000000000000003
```

The initial implementation never reports deleted files because physical
deletion is unavailable. Archive operations are moves.

## 21. Release publication

Each successful build is immutable:

```text
releases/
├── by-id/
│   ├── 20260731T040000Z-<commit>/
│   └── 20260731T040500Z-<commit>/
└── current -> by-id/20260731T040500Z-<commit>/
```

The Worker:

1. builds in `work/`;
2. verifies the output and associated commit ID;
3. moves it into `releases/by-id/`;
4. creates a temporary `current` symlink;
5. atomically renames the symlink into place; and
6. synchronizes the releases directory.

If a build or activation fails, the previous `current` release remains
available.

Release retention is configurable. Removal of old derived releases is an
administrative maintenance operation and never removes content or accepted
requests.

## 22. Completion and replication

A request becomes `completed` after:

- its commit is on the official local branch;
- `content/` can be repaired or synchronized to that commit; and
- the Quartz release for that commit is active.

Remote Git push is deliberately outside this completion boundary. Its status
is recorded separately and retried with bounded exponential backoff. This
prevents a remote outage from making healthy local knowledge unavailable.

## 23. Reads

Human users read the active Quartz site.

Coding agents and shell users read through the Gateway. Initial read
operations include:

- directory listing;
- recent documents;
- document lookup by ID;
- Markdown retrieval;
- document-bundle export;
- project filtering;
- tag filtering;
- session lookup; and
- request-status lookup.

Each content read pins the official commit at operation start. Pending and
processing data is visible only through status operations, never through
normal content reads.

## 24. Search

The initial search implementation scans committed Markdown and permitted
front-matter fields. It searches:

- titles;
- Markdown bodies;
- paths;
- tags; and
- configured metadata fields.

It does not search:

- pending request contents;
- generated Quartz HTML;
- Git internal files;
- binary attachment contents; or
- PDF or standalone HTML bodies.

Search is exposed behind an internal Rust trait so that a dedicated text or
vector index can be added later. An index is always derived data and must be
associated with an exact commit ID.

## 25. Failure classification

Permanent request failures include:

- malformed request or front matter;
- reused request ID with different content;
- invalid or escaping path;
- unsupported file type;
- size or count limit violation;
- duplicate document ID;
- optimistic-lock conflict;
- forbidden log mutation; and
- deterministic Quartz failure caused by the request.

Transient failures include:

- temporary file-system or process errors;
- resource exhaustion that may clear;
- interrupted Worker execution;
- temporary Quartz process failure; and
- Git remote unavailability.

A failed request is retained:

```json
{
  "schema_version": 1,
  "request_id": "01K00000000000000000000000",
  "batch_id": "01K00000000000000000000010",
  "status": "failed",
  "error_code": "REVISION_CONFLICT",
  "failed_at": "2026-07-31T04:00:00+09:00"
}
```

Human-readable error text may accompany the stable error code, but clients
must branch on the code.

## 26. Initial error codes

```text
INVALID_PROTOCOL
INVALID_REQUEST
INVALID_FRONT_MATTER
INVALID_PATH
UNSUPPORTED_FILE_TYPE
LIMIT_EXCEEDED
REQUEST_ID_REUSED
DOCUMENT_ID_CONFLICT
DOCUMENT_NOT_FOUND
REVISION_CONFLICT
OPERATION_FORBIDDEN
CONTENT_VALIDATION_FAILED
QUARTZ_BUILD_FAILED
TEMPORARY_FAILURE
INTERNAL_ERROR
```

Error codes may be added compatibly. Existing meanings must not change within
a protocol major version.

## 27. Configuration

Configuration, rather than architecture, controls:

- storage paths;
- debounce interval;
- maximum batch age;
- maximum requests per batch;
- maximum request bytes;
- maximum individual file bytes;
- maximum file count;
- maximum directory and total package-entry counts;
- maximum payload path components;
- maximum front-matter bytes;
- incoming quarantine and reap age thresholds;
- allowed attachment extensions;
- project identifiers;
- document types;
- Git remote name and branch;
- Quartz command and configuration;
- release retention;
- retry limits and backoff;
- title length;
- search backend; and
- CLI output format.

Configuration uses a versioned, human-readable file. Secret values are
referenced through deployment-provided files or environment variables and are
never written to logs or committed examples.

Unknown configuration fields fail startup unless a later compatibility policy
explicitly permits them.

## 28. Security

Both the Gateway and Worker treat every request and archive entry as
untrusted. Defense in depth includes:

- OpenSSH public-key authentication;
- one client identity per key;
- forced commands and disabled forwarding;
- no shell evaluation;
- exact protocol-version and operation allowlists;
- strict JSON and tar limits;
- duplicate-key and duplicate-path rejection;
- shared path validation at enqueue and apply time;
- rejection of links and special files;
- safe process argument construction;
- execution timeouts and output limits for Git and Quartz;
- least-privilege service accounts;
- no credentials in request packages, content, logs, or commits; and
- audit records containing client ID and request ID.

HTML and SVG attachments may contain active content. They are stored as
attachments and never executed by the Worker. The external Web-serving layer
is responsible for an appropriate content security policy, content types, and
download or origin isolation.

## 29. Observability

Processes emit structured logs with:

- timestamp;
- severity;
- component;
- request ID;
- client ID where applicable;
- batch or release ID;
- stable event or error code; and
- retry attempt.

Logs must not contain document bodies, attachment contents, private keys,
tokens, or Git credentials.

Administrative status reports include:

- queue counts and oldest age;
- current commit and release;
- Worker lock and last successful batch;
- remote replication lag; and
- recent permanent and transient failure counts.

## 30. Testing

The implementation requires:

- unit tests for schema, path, revision, and state-transition logic;
- property tests for path and archive-entry validation;
- tests for duplicate and reordered tar entries;
- integration tests using temporary Git repositories;
- deterministic fake Quartz commands for success and failure;
- crash-injection tests at durable queue and publication boundaries;
- recovery tests for every processing phase;
- batching tests with a mixture of valid, conflicting, and invalid requests;
- protocol compatibility fixtures; and
- end-to-end tests through a restricted OpenSSH configuration where the test
  environment permits it.

Tests and examples use clearly fictional identities and infrastructure values.

## 31. Delivery sequence

Implementation proceeds in these increments:

1. Rust workspace, shared domain types, and validation.
2. Durable file queue and crash-recovery tests.
3. Local administrative submission and Worker transaction.
4. Git worktrees, commits, and optimistic locking.
5. Quartz trial builds and atomic releases.
6. OpenSSH forced-command Gateway and client SSH transport.
7. Committed reads and initial full-text search.
8. Remote push retry, operational status, and retention.
9. Optional container and single-replica Kubernetes packaging.

Every increment keeps `just check` passing and preserves the invariants in this
document.
