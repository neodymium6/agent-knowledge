[package]
name = "agent-knowledge-release"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
publish = false

[lints]
workspace = true

[dependencies]
agent-knowledge-core = { path = "../core" }
serde.workspace = true
serde_json.workspace = true
sha2.workspace = true
time.workspace = true
ulid.workspace = true

[target.'cfg(unix)'.dependencies]
nix = { version = "0.30.1", features = ["dir", "fs", "process", "signal", "user"] }
